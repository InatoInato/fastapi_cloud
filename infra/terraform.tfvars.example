# Production Terraform Configuration Example
# Copy to terraform.tfvars and update with your values

allowed_ssh_cidr = "YOUR.IP.ADDRESS/32"  # Change to your IP, NOT 0.0.0.0/0 in production
instance_type   = "t3.small"              # t3.micro for testing only
environment     = "prod"
